apiVersion: v1
kind: Service
metadata:
  name: {{ include "sequence-forge.fullname" . }}
  labels:
    {{- include "sequence-forge.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sequence-forge.selectorLabels" . | nindent 4 }}
